input format would be a username and output would the the password of that username 
using brute-force method(checking for all possoble combinations of the password). This 
brute froce would be divided into different functions so the task can be done in parallel
in a much master way. For execution we need to have mpi.h header file included.